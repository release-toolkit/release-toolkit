{
  "name": "release-toolkit",
  "private": true,
  "type": "module",
  "description": "CI-driven release CLI — version detection + changelog generation with plugin system",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "format": "prettier --write \"packages/*/src/**/*.ts\"",
    "release": "node packages/cli/dist/index.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "keywords": [
    "monorepo",
    "release",
    "changelog",
    "version",
    "ci",
    "semver"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.0",
    "tsdown": "^0.21.10",
    "vitest": "^4.1.5",
    "eslint": "^9.0.0",
    "prettier": "^3.8.2",
    "@eslint/js": "^9.0.0",
    "globals": "^15.0.0",
    "typescript-eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0"
  }
}
